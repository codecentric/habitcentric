fullnameOverride: ui
image:
  repository: denniseffing/hc-ui
  pullPolicy: Always

oidc:
  {{- if or (eq .Environment.Name "istio") (eq .Environment.Name "linkerd") }}
  enabled: true
  authorityUri: habitcentric.demo/auth/realms/habitcentric/.well-known/openid-configuration
  {{- else }}
  enabled: false
  authorityUri: keycloak.demo/auth/realms/habitcentric/.well-known/openid-configuration
  {{- end }}
  clientId: ui