apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: habitcentric
  name: track-db-pv-claim
  labels:
    app: habitcentric
    component: track-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi