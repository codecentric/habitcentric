.docker-build:
  needs:
    - cd "$SERVICE_FOLDER"
  services:
    - docker:dind
  stage: build image
  image: docker:latest
  before_script:
    - cd "$SERVICE_FOLDER"
  script:
    - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA .
    - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA

.docker-publish:
  only:
    refs:
      - main
  stage: publish image
  image: docker:lastest
  services:
    - docker:dind
  before_script:
    - cd "$SERVICE_FOLDER"
  script:
    - docker login -u $DOCKER_HUB_REGISTRY_USER -p $DOCKER_HUB_REGISTRY_PASSWORD $DOCKER_HUB_REGISTRY
    - docker pull $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG
    - docker tag $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG $DOCKER_HUB_REGISTRY_IMAGE:latest
    - docker push $DOCKER_HUB_REGISTRY_IMAGE:latest
