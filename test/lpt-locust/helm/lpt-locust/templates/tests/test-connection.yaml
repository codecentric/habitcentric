apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lpt-locust.fullname" . }}-test-connection"
  labels:
    {{- include "lpt-locust.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lpt-locust.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
